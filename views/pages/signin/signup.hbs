<!DOCTYPE html>
<html lang="en">
    <head>
        {{> head}}
        <title>FOCCACIA | Sign Up</title>
        {{> script}}
    </head>
    <body class="w-full min-h-full flex flex-col justify-center items-center">
        {{#if error}}
            {{#> sonner}}
                <p class="font-semibold">Signup failed!</p>
                <p class="mt-1 text-sm text-stone-700">An unknown error occurred while signing up</p>
            {{/sonner}}
        {{/if}}
        {{#> signin btnText="Sign Up" method="POST" action="/signup"}}
            Already have an account? {{#> button link=true anchor=true href="/login"}}Log in{{/button}} instead
        {{/signin}}
    </body>
    <script>
        const params = new URLSearchParams(window.location.search);
        params.delete("error");
        window.history.replaceState({}, "", window.location.pathname + (params.size != 0 ? "?" + params.toString() : ""));
    </script>
</html>