<!DOCTYPE html>
<html lang="en">
    <head>
        {{> head}}
        <title>FOCCACIA | Create Group</title>
    </head>
    <body>
        {{> navbar fixed=true}}
        <div class="w-full h-full flex flex-col justify-center items-center">
            <div class="w-full max-w-sm my-20 flex flex-col">
                <h1 class="mb-6 text-3xl font-bold">Create Group</h1>
                <form method="post" class="w-full flex flex-col space-y-4">
                    {{> input type="text" name="name" placeholder="Name" required="required"}}
                    {{> input type="area" name="description" placeholder="Description" required="required"}}
                    {{#> button submit=true}}Create{{/button}}
                </form>
            </div>
        </div>
    </body>
</html>