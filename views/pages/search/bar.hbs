<!DOCTYPE html>
<html lang="en">
    <head>
        {{> head}}
        <title>FOCCACIA | Search Teams</title>
    </head>
    <body>
        {{> navbar fixed=true loggedIn=loggedIn}}
        <div class="w-full min-h-full flex flex-col justify-center items-center">
            {{#if failSearch}}
                <div class="h-10"></div>
            {{/if}}
            <h1 class="mb-14 text-6xl font-extrabold">Search Teams</h1>
            <form method="get" class="w-120">
                <div class="w-full flex justify-between items-center text-xl bg-stone-100 border-2 border-black/15 focus-within:border-orange-600 rounded-xl shadow-md ring-0 focus-within:ring-4 ring-orange-600/20 focus-within:outline-0 transition duration-200">
                    <input type="text" name="team" placeholder="Manchester United" required="required" class="w-full px-4 py-3 outline-0" />
                    {{#> button class="h-full m-1.5 !p-2"}}
                        {{> search28 class="w-7 h-7"}}
                    {{/button}}
                </div>
                {{#if failSearch}}
                    <p class="w-full mt-3 ml-2 text-lg text-red-600 font-semibold">No teams were found for "{{query}}"</p>
                {{/if}}
            </form>
        </div>
    </body>
</html>